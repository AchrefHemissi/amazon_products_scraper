# Docker-specific environment settings for local MongoDB
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example
MONGO_INITDB_DATABASE=amazon_scraper

# Example MONGO_URI you can use with the project once the container is up:
# MONGO_URI=mongodb://root:example@localhost:27017/?authSource=admin
